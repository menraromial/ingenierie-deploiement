# Dépendances (réinstallées par `npm ci`)
node_modules/

# Sorties de build Docusaurus (générées par la CI, jamais versionnées)
build/
.docusaurus/
.cache-loader/

# Build intermédiaire LaTeX des figures (les SVG/PDF de static/figures, eux, sont versionnés)
figures/build/

# Photo source haute définition (la version recadrée est dans static/img/)
profile.jpg

# Journaux et environnement local
npm-debug.log*
.env.local
.env.*.local

# Éditeurs / OS
.idea/
.vscode/
*.swp
.DS_Store

# Caches Python (sources des kits de TP)
__pycache__/
